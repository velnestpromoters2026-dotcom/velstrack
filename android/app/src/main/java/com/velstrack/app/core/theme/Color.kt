package com.velstrack.app.core.theme

import androidx.compose.ui.graphics.Color

// Premium Executive Monochrome Brand Colors
val PureWhite = Color(0xFFFFFFFF)
val MetallicSilver = Color(0xFFE0E0E0)
val DarkSilver = Color(0xFFA0A0A0)

// Background & Surface Colors
val AbsoluteBlack = Color(0xFF000000)
val ObsidianBlack = Color(0xFF0A0A0A)
val SurfaceGray = Color(0xFF1A1A1A)
val ElevatedGray = Color(0xFF242424)

// Semantic Colors (Subdued for the monochrome theme)
val EmeraldSuccess = Color(0xFF10B981) // Keep standard for indicators
val AmberWarning = Color(0xFFF59E0B)
val RoseDanger = Color(0xFFE11D48)

// Alpha/Glass Colors
val GlassSurface = Color(0xFF1A1A1A).copy(alpha = 0.8f)
val GlassBorder = Color(0xFFFFFFFF).copy(alpha = 0.15f)
